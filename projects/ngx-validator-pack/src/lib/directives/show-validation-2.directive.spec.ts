import { ShowValidation2Directive } from './show-validation-2.directive';

describe('ShowValidation2Directive', () => {
  it('should create an instance', () => {
    const directive = new ShowValidation2Directive();
    expect(directive).toBeTruthy();
  });
});

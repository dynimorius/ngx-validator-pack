import { ChecksDirective } from './checks.directive';

describe('CheckedValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new ChecksDirective();
    expect(directive).toBeTruthy();
  });
});

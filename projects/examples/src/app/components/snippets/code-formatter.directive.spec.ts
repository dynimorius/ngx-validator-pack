import { CodeFormatterDirective } from './code-formatter.directive';

describe('CodeFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new CodeFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
